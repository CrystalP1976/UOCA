<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UOCApp.LoginPage">

  <StackLayout       VerticalOptions="FillAndExpand"
                     HorizontalOptions="FillAndExpand"
                     Orientation="Vertical"
                     Spacing="15"
                     Padding="10,0,10,0">

    <Label Text="Login Page" VerticalOptions="Center" HorizontalOptions="Center" />

    <Grid Padding="40,20">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Label Text="Password:" Grid.Row="0" Grid.Column="0" FontSize="Large" HorizontalTextAlignment="End"/>
      <Entry Grid.Row="0" Grid.Column="1" HorizontalOptions="FillAndExpand"/>

      <Button Text="Login"
           HorizontalOptions="CenterAndExpand"
              Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"
           />

    </Grid>

  </StackLayout>
	
</ContentPage>